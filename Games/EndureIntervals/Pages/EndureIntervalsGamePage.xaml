<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:fApp.Games.EndureIntervals"
             x:Class="fApp.Games.EndureIntervals.EndureIntervalsGamePage"
             NavigationPage.HasNavigationBar="False">

    <Grid Padding="18"
          RowDefinitions="Auto,Auto,*,Auto"
          RowSpacing="14">

        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
            <Button Grid.Column="0"
                    Text="{x:Static local:EndureIntervalsResources.Game_End}"
                    WidthRequest="76"
                    HeightRequest="76"
                    CornerRadius="999"
                    Padding="0"
                    FontSize="22"
                    BackgroundColor="{StaticResource ButtonColor}"
                    TextColor="{StaticResource White}"
                    Command="{Binding EndCommand}" />

            <Image Grid.Column="1"
                   Source="smoke_logo.svg"
                   HeightRequest="76"
                   Aspect="AspectFit"
                   HorizontalOptions="Center" />

            <Button Grid.Column="2"
                    Text="{x:Static local:EndureIntervalsResources.Game_Reset}"
                    WidthRequest="76"
                    HeightRequest="76"
                    CornerRadius="999"
                    Padding="0"
                    FontSize="22"
                    BackgroundColor="{StaticResource ButtonColor}"
                    TextColor="{StaticResource White}"
                    Command="{Binding ResetCommand}" />
        </Grid>

        <Grid Grid.Row="1" ColumnDefinitions="*,*" ColumnSpacing="12">
            <Border StrokeThickness="0" BackgroundColor="{StaticResource ButtonColor}" StrokeShape="RoundRectangle 18" Padding="14,12">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Timer1" FontSize="28" />
                    <Label Text="{Binding Timer1RemainingText}" FontSize="44" />
                    <Label Text="{Binding Timer1RepeatText}" FontSize="22" />
                </VerticalStackLayout>
            </Border>

            <Border Grid.Column="1" StrokeThickness="0" BackgroundColor="{StaticResource ButtonColor}" StrokeShape="RoundRectangle 18" Padding="14,12">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Timer2" FontSize="28" />
                    <Label Text="{Binding Timer2RemainingText}" FontSize="44" />
                    <Label Text="{Binding Timer2EnabledText}" FontSize="22" />
                </VerticalStackLayout>
            </Border>
        </Grid>

        <Border Grid.Row="2" StrokeThickness="0" BackgroundColor="{StaticResource ButtonColor}" StrokeShape="RoundRectangle 18" Padding="16,12">
            <Label Text="{Binding StatusText}" FontSize="28" />
        </Border>

        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="12">
            <Button Grid.Column="0"
                    Text="{Binding StartPauseText}"
                    HeightRequest="72"
                    CornerRadius="18"
                    FontSize="28"
                    Command="{Binding StartPauseCommand}" />
            <Button Grid.Column="1"
                    Text="{x:Static local:EndureIntervalsResources.Game_Pause}"
                    HeightRequest="72"
                    CornerRadius="18"
                    FontSize="28"
                    IsVisible="False" />
        </Grid>

    </Grid>
</ContentPage>
