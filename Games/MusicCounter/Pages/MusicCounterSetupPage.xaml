<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:fApp.Games.MusicCounter"
             x:Class="fApp.Games.MusicCounter.MusicCounterSetupPage"
             NavigationPage.HasNavigationBar="False">

    <Grid Padding="24"
          RowDefinitions="Auto,*,Auto"
          RowSpacing="18">

        <Image Grid.Row="0"
               Source="suc_logo.svg"
               HeightRequest="200"
               Aspect="AspectFit"
               HorizontalOptions="Center"
               Margin="0,8,0,6" />

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Categories}"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="14" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto,Auto"
                          ColumnSpacing="10"
                          RowDefinitions="Auto">

                        <Border Grid.Column="0"
                                StrokeThickness="0"
                                Padding="16,12"
                                StrokeShape="RoundRectangle 18">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:MusicCounterSetupViewModel}}, Path=ToggleSelectedCommand}"
                                                      CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>

                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="BackgroundColor" Value="{StaticResource SelectedColor}" />
                                </DataTrigger>
                                <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="False">
                                    <Setter Property="BackgroundColor" Value="{StaticResource ButtonColor}" />
                                </DataTrigger>
                            </Border.Triggers>

                            <Grid ColumnDefinitions="*,Auto">
                                <Label Text="{Binding DisplayName}"
                                       FontSize="24"
                                       HorizontalTextAlignment="Start"
                                       VerticalTextAlignment="Center" />

                                <Label Grid.Column="1"
                                       Text="{Binding DrawCount}"
                                       FontSize="24"
                                       VerticalTextAlignment="Center"
                                       HorizontalTextAlignment="End" />
                            </Grid>
                        </Border>

                        <Button Grid.Column="1"
                                Text="-"
                                WidthRequest="56"
                                HeightRequest="56"
                                CornerRadius="18"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:MusicCounterSetupViewModel}}, Path=DecrementCommand}"
                                CommandParameter="{Binding .}" />

                        <Button Grid.Column="2"
                                Text="+"
                                WidthRequest="56"
                                HeightRequest="56"
                                CornerRadius="18"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:MusicCounterSetupViewModel}}, Path=IncrementCommand}"
                                CommandParameter="{Binding .}" />

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2"
                Text="{x:Static local:MusicCounterResources.Setup_Go}"
                BackgroundColor="{StaticResource SpecialColor}"
                CornerRadius="999"
                HeightRequest="96"
                FontSize="42"
                Margin="0,14,0,0"
                IsEnabled="{Binding CanStart}"
                Command="{Binding GoCommand}" />

    </Grid>
</ContentPage>
